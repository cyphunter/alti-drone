# PROJECT_NAME — instructions Claude (spécifiques projet)

> Règles agence : voir `../CLAUDE.md` à la racine de `freelance/`.
> Référence détaillée : voir `../CONVENTIONS.md`.

## Identité du projet

- **Client** : `<À RENSEIGNER>`
- **Activité** : `<À RENSEIGNER>`
- **Site** : `https://PROJECT.fr`
- **SITE_ID** : `PROJECT_SITE_ID` (préfixe tables D1)
- **D1** : partagée `freelance-shared` (tables préfixées)
- **R2 bucket** : `PROJECT_NAME-media`
- **Email expéditeur** : `<service>@PROJECT.fr` (Resend)

## Direction artistique

- Palette : `<à compléter>`
- Polices : Inter (body) + Fraunces (display) — modifier dans `src/app/layout.tsx` si besoin
- Ton : `<éditorial / familier / institutionnel ?>`

## Sources de contenu client-éditables

- **`src/lib/site-config.ts`** — coordonnées, navigation, mentions légales, social
- **`src/data/*.ts`** (à créer selon le projet) — services, réalisations, FAQ, etc.

## Points d'attention spécifiques

- `<À COMPLÉTER selon les particularités du client>`
